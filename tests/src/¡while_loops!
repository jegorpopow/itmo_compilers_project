routine collatz(n : integer) : integer is
  var steps is 0;

  while n /= 1 loop
    if n % 2 = 0 then
      n := n / 2;
    else
      n := 3 * n + 1;
    fi
    steps := steps + 1;
  pool

  return steps;
enitour

routine main() is
  print collatz(7);
enitour
